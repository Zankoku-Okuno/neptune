language: haskell
ghc:
    - 7.6

before_install:
    - cd neptune/
    - cabal install --only-dependencies --enable-tests
script:
    - cabal-meta install --enable-tests

notifications:
    email:
        on_success: never
        on_failure: change