TODO
	logging (to file, email admin)
	sql query formatting (string + parameter merging)
	sql marshalling
	custom error responses
    infodump on error in debug mode
	formatting
		html document header
		html vocabulary extension
		add'l formats
	templates


FIXME
	setup.py
	content negotation is not fully compliant
		q=0.0
		parameters beyond q
        strings in parameters
    sendfile guess content type


FLESH OUT
	route segments
	HTML model


CONFIG
	recognized content-types
	allowed HTTP methods
	debug
	error responses
	sendfile chunk size
